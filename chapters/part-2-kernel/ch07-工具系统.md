# Chapter 7 工具系统

## 44 个工具全景

| 功能域 | ReadOnly | SideEffect | Exclusive |
| --- | --- | --- | --- |
| 文件（6） | `read_file` `glob` `grep` `browser` | `write_file` `edit_file` | — |
| 命令（1） | — | — | `bash`（高风险，120s 超时，sandbox） |
| 网络（2） | `web_fetch` `web_search` | — | — |
| 记忆（5） | `read_memory` `structured_memory_search` | `write_memory` `structured_memory_update/delete` | — |
| 调度（8） | `list_scheduled_tasks` `get_task_history` `list_scheduled_task_dlq` | `schedule_task` `pause/resume/cancel_*` `replay_task_dlq` | — |
| 协作（3） | `export_chat` `todo_read` | `send_message`（连续 3 次阻断）`todo_write` | — |
| 子代理（11） | `subagents_list/info/focused/log` | `kill/focus/unfocus/send/orchestrate/retry` | `sessions_spawn` |
| 时间（3） | `get_current_time` `compare_time` `calculate` | — | — |
| 技能（2） | — | `sync_skills` | `activate_skill` |
| A2A+ClawHub（4） | `a2a_list_peers` `clawhub_search` | `a2a_send` `clawhub_install` | — |

文件操作支持 `working_dir_isolation`（默认 Chat 级隔离）。

## `Tool` 抽象

```rust
struct ToolSpec {
    name: String,
    description: String,
    input_schema: serde_json::Value,
}

#[async_trait::async_trait]
trait Tool: Send + Sync {
    fn spec(&self) -> ToolSpec;
    async fn execute(&self, input: serde_json::Value) -> ToolResult;
}
```

```python
from dataclasses import dataclass
from typing import Any, Protocol


@dataclass
class ToolSpec:
    name: str
    description: str
    input_schema: dict[str, Any]


class Tool(Protocol):
    def spec(self) -> ToolSpec: ...
    async def execute(self, input: dict[str, Any]) -> ToolResult: ...
```

## `ToolRegistry`：治理入口

- **自动注入 chat_id**：模型没传 chat_id 时从 `ToolAuthContext.caller_chat_id` 注入——模型不能被信任来声明权限边界。
- **授权上下文**：`ToolAuthContext`（`caller_channel`、`caller_chat_id`、`control_chat_ids`）注入到 `__microclaw_auth`，权限来自运行时事实。
- **Sandbox 路由**：`SandboxRouter` 决定宿主机 vs 容器执行，管理安全 profile。
- **子代理受限工具集**：`new_sub_agent` 不含 `write_memory`、`send_message`、`schedule_*` 等，防止递归失控。

## Wave-Based 并行执行

### 三级并发分类

```rust
pub enum ToolConcurrencyClass {
    ReadOnly,    // 可以和其他 ReadOnly 并行
    SideEffect,  // 必须和其他 SideEffect/Exclusive 串行
    Exclusive,   // 必须独占执行
}
```

### 分区规则

```
模型返回多个 tool_use → 按 concurrency class 分区：
  Wave 1: [所有 ReadOnly]  ── 并行执行
  Wave 2: [SideEffect #1]  ── 独占
  Wave 3: [SideEffect #2]  ── 独占
  Wave N: [Exclusive #1]   ── 独占
```

例：`read_file` + `grep` + `web_fetch` + `write_file` + `bash` → Wave 1: 前三个并行 → Wave 2: write_file → Wave 3: bash

### 单工具执行链路

```
名字检查 → send_message 限制 → BeforeToolCall hook
  → ToolStart 事件 → 执行工具 → OTLP trace
    → approval_required 自动重试 → activate_skill metadata
      → AfterToolCall hook → ToolResult 事件 → 更新错误跟踪
```

```{=typst}
#pagebreak(weak: true)
```

## 示例代码：wave-based 并行执行

```rust
#[derive(Clone, Copy, PartialEq)]
enum ConcurrencyClass {
    ReadOnly,
    SideEffect,
    Exclusive,
}

fn partition_into_waves(
    calls: &[ToolCall],
    classify: impl Fn(&str) -> ConcurrencyClass,
) -> Vec<Vec<usize>> {
    let mut readonly = Vec::new();
    let mut side_effect = Vec::new();
    let mut exclusive = Vec::new();
    for (i, call) in calls.iter().enumerate() {
        match classify(&call.name) {
            ConcurrencyClass::ReadOnly => readonly.push(i),
            ConcurrencyClass::SideEffect => side_effect.push(i),
            ConcurrencyClass::Exclusive => exclusive.push(i),
        }
    }
    let mut waves = Vec::new();
    if !readonly.is_empty() { waves.push(readonly); }
    for idx in side_effect { waves.push(vec![idx]); }
    for idx in exclusive { waves.push(vec![idx]); }
    waves
}
```

```rust

async fn execute_batch(tools: &ToolRegistry, calls: &[ToolCall]) -> Vec<ToolResult> {
    let waves = partition_into_waves(calls, |name| tool_concurrency_class(name));
    let mut all_results = Vec::new();
    for wave in &waves {
        if wave.len() == 1 {
            let result = tools.execute(&calls[wave[0]]).await;
            all_results.push((wave[0], result));
        } else {
            let mut join_set = tokio::task::JoinSet::new();
            for &idx in wave {
                let call = calls[idx].clone();
                join_set.spawn(async move { (idx, tools.execute(&call).await) });
            }
            while let Some(Ok(result)) = join_set.join_next().await {
                all_results.push(result);
            }
        }
    }
    all_results.sort_by_key(|(idx, _)| *idx);
    all_results.into_iter().map(|(_, r)| r).collect()
}
```

```{=typst}
#pagebreak(weak: true)
```

```python
from collections.abc import Callable
from enum import Enum


class ConcurrencyClass(Enum):
    READ_ONLY = "read_only"
    SIDE_EFFECT = "side_effect"
    EXCLUSIVE = "exclusive"


def partition_into_waves(
    calls: list["ToolCall"],
    classify: Callable[[str], ConcurrencyClass],
) -> list[list[int]]:
    readonly, side_effect, exclusive = [], [], []
    for i, call in enumerate(calls):
        cls = classify(call.name)
        if cls == ConcurrencyClass.READ_ONLY:
            readonly.append(i)
        elif cls == ConcurrencyClass.SIDE_EFFECT:
            side_effect.append(i)
        else:
            exclusive.append(i)
    waves = []
    if readonly:
        waves.append(readonly)
    waves.extend([idx] for idx in side_effect)
    waves.extend([idx] for idx in exclusive)
    return waves
```

```python

async def execute_batch(tools: "ToolRegistry", calls: list["ToolCall"]) -> list["ToolResult"]:
    import asyncio

    waves = partition_into_waves(calls, tool_concurrency_class)
    all_results: list[tuple[int, "ToolResult"]] = []
    for wave in waves:
        if len(wave) == 1:
            result = await tools.execute(calls[wave[0]])
            all_results.append((wave[0], result))
        else:
            tasks = [tools.execute(calls[idx]) for idx in wave]
            results = await asyncio.gather(*tasks)
            all_results.extend(zip(wave, results))
    all_results.sort(key=lambda x: x[0])
    return [r for _, r in all_results]
```

## Hook：可插拔治理层

| 事件 | 能力 |
| --- | --- |
| `BeforeLLMCall` | block 请求或修改 system prompt |
| `BeforeToolCall` | block 工具调用或修改输入 |
| `AfterToolCall` | block 结果（标记错误）或修改 output |

`HookOutcome` = `Allow { patches }` 或 `Block { reason }`。适合承载部署级策略（风险摘要、临时禁用、脱敏），不污染工具实现。

## 关键权衡

| 决策 | 优点 | 代价 |
| --- | --- | --- |
| wave-based 分区 | 安全可控 | 无法做更细粒度依赖分析 |
| concurrency class 硬编码+可覆盖 | 默认安全 | 新工具需手动添加分类 |
| 授权由 runtime 注入 | 权限可信 | 工具接口稍复杂 |
| Hook 承载部署级策略 | 不污染工具实现 | 需自身的文档和故障隔离 |
| 子代理受限工具集 | 防递归失控 | 能力受限 |

## 容易走错的地方

1. **把工具当简单函数调用**：忽视 schema、审计、权限、超时、concurrency class 和 hook。
2. **相信模型参数可信**：模型生成的 chat_id、路径不应直接当事实。`inject_default_chat_id_if_missing` 和 `authorize_chat_access` 存在的意义正在于此。
3. **忽视并行正确性**：把所有工具标为 ReadOnly → write_file 和 bash 并发导致文件损坏。
4. **把 sandbox 当唯一安全手段**：没有审批、路径限制、授权和 Hook，sandbox 仍留大量盲区。

## 小结

44 个工具做成统一授权、并行执行、审批隔离、审计和策略扩展的能力层。wave-based 引擎让"读取并行、写入串行、高危独占"成为可编程调度策略。

## 证据来源（v0.1.38）

`src/tools/mod.rs`、`src/tool_executor.rs`（877 行）、`crates/microclaw-tools/src/runtime.rs`、`src/hooks.rs`。关键配置：`parallel_tool_max_concurrency=8`、`working_dir_isolation=Chat`

## 图表清单

### 图 7-1：工具治理链路

![图 7-1：工具治理链路](../assets/figures/fig-07-tool-governance.svg)
