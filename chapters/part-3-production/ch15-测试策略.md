# Chapter 15 测试策略

## 确定性与非确定性的分治

Agent Runtime 同时包含确定性部分（数据库、权限、配置、并发控制）和非确定性部分（模型输出、长对话）。用测 CRUD 的方式测 Agent，要么测不到关键路径，要么全是脆弱断言。

## 依赖审计：deny.toml

### 安全公告

```toml
[advisories]
version = 2
yanked = "deny"
ignore = ["RUSTSEC-2026-0049"]  # 临时豁免等待上游修复
```

### 许可证合规

```toml
[licenses]
allow = [
  "MIT", "Apache-2.0", "BSD-2-Clause", "BSD-3-Clause",
  "ISC", "MPL-2.0", "Zlib", ...
]
confidence-threshold = 0.8
```

### 依赖重复检查

```toml
[bans]
multiple-versions = "allow"
wildcards = "deny"
```

`wildcards = "deny"` 禁止 `*` 版本通配，防止不可预测的版本漂移。

## CI/CD 门禁

```
cargo fmt --check → cargo clippy --all-targets → cargo test
→ cargo deny check → Web 前端构建 → 文档生成物检查
```

## 并行工具执行的测试

### Wave 分区正确性（纯确定性）

- 全 ReadOnly → 单 wave 并行
- 混合 ReadOnly + SideEffect → ReadOnly 先并行，SideEffect 后顺序
- Exclusive → 独占 wave
- `tool_concurrency_overrides` 是否生效

### 并行状态安全

- 并行工具结果不互相覆盖
- 一个工具失败不影响其他并行工具
- `waiting_for_user_approval` 并行语义正确

### ToolWave 事件完整性

- 事件顺序：WaveStart(1) → WaveComplete(1) → WaveStart(2) → ...
- tool_count 准确、duration_ms 合理

### 测试分区逻辑而非"并行本身"

```rust
#[derive(Clone, Debug, PartialEq)]
enum ConcurrencyClass { ReadOnly, SideEffect, Exclusive }

fn partition_into_waves(classes: &[ConcurrencyClass]) -> Vec<Vec<usize>> {
    let mut readonly = Vec::new();
    let mut sideeffect = Vec::new();
    let mut exclusive = Vec::new();
    for (i, c) in classes.iter().enumerate() {
        match c {
            ConcurrencyClass::ReadOnly => readonly.push(i),
            ConcurrencyClass::SideEffect => sideeffect.push(i),
            ConcurrencyClass::Exclusive => exclusive.push(i),
        }
    }
    let mut waves = Vec::new();
    if !readonly.is_empty() { waves.push(readonly); }
    for idx in sideeffect { waves.push(vec![idx]); }
    for idx in exclusive { waves.push(vec![idx]); }
    waves
}

#[test]
fn test_mixed_partition() {
    use ConcurrencyClass::*;
    let classes = vec![ReadOnly, ReadOnly, SideEffect, Exclusive];
    let waves = partition_into_waves(&classes);
    assert_eq!(waves, vec![vec![0, 1], vec![2], vec![3]]);
}
```

```python
from enum import Enum


class ConcurrencyClass(str, Enum):
    READ_ONLY = "read_only"
    SIDE_EFFECT = "side_effect"
    EXCLUSIVE = "exclusive"

def partition_into_waves(classes: list[ConcurrencyClass]) -> list[list[int]]:
    readonly = [i for i, c in enumerate(classes) if c == ConcurrencyClass.READ_ONLY]
    sideeffect = [i for i, c in enumerate(classes) if c == ConcurrencyClass.SIDE_EFFECT]
    exclusive = [i for i, c in enumerate(classes) if c == ConcurrencyClass.EXCLUSIVE]
    waves: list[list[int]] = []
    if readonly:
        waves.append(readonly)
    waves.extend([idx] for idx in sideeffect)
    waves.extend([idx] for idx in exclusive)
    return waves

def test_mixed_partition() -> None:
    classes = [
        ConcurrencyClass.READ_ONLY, ConcurrencyClass.READ_ONLY,
        ConcurrencyClass.SIDE_EFFECT, ConcurrencyClass.EXCLUSIVE,
    ]
    assert partition_into_waves(classes) == [[0, 1], [2], [3]]
```

## 子代理生命周期测试

| 路径 | 验证点 |
|------|--------|
| 正常完成 | ACP 握手、任务执行、`subagent_runs` status=completed、token/成本记录 |
| 超时 | 进程终止、status=timeout、父 agent 不崩溃 |
| 取消 | `local_cancel` 设置、ACP 检测停止、进程清理 |
| 并发限制 | 达到 `max_concurrent` 时等待或拒绝、无死锁 |

## per-chat Turn Serialization 测试

```rust
#[tokio::test]
async fn test_acquire_release_basic() {
    let q = make_queue();
    let guard = q.acquire("telegram", 1).await;
    assert!(guard.is_some());
    drop(guard);
    let guard2 = q.acquire("telegram", 1).await;
    assert!(guard2.is_some());
}
```

关键验证：并发阻塞、不同 chat 独立、`max_pending` 限制、`try_start_or_enqueue` 原子性。

## 集成测试覆盖

| 测试文件 | 覆盖范围 | 为什么关键 |
|---------|---------|-----------|
| `config_validation.rs` | 最小配置、默认值、完整 YAML、边界值 | 配置解释与默认值漂移 |
| `tool_permissions.rs` | `ToolAuthContext`、跨 chat 访问控制 | 权限回归后果严重 |
| `db_integration.rs` | 消息、session、scheduled tasks、`subagent_runs` | 跨表生命周期正确性 |

## 对 LLM 非确定性：测结构不测文风

**该测**：正确工具分支、`stop_reason` 安全处理、审批状态机、工具失败后会话恢复、compaction 后消息结构合法、wave 分区正确

**不该测**：最终回答是否一字不差

```{=typst}
#pagebreak(weak: true)
```

## 示例代码：run_control 取消传播

```rust
#[tokio::test]
async fn test_abort_runs_marks_cancelled() {
    let (run_id, cancelled, _notify) =
        register_run("test", 101, Some("u1".to_string())).await;
    assert!(!is_cancelled(&cancelled));

    let aborted = abort_runs("test", 101).await;
    assert_eq!(aborted, 1);
    assert!(is_cancelled(&cancelled));
    assert!(is_aborted_source_message("test", 101, "u1").await);

    unregister_run("test", 101, run_id).await;
}
```

```python
import asyncio
from dataclasses import dataclass, field
from threading import Event


@dataclass
class RunControl:
    cancelled: Event = field(default_factory=Event)

async def test_abort_propagates() -> None:
    rc = RunControl()
    assert not rc.cancelled.is_set()
    rc.cancelled.set()
    assert rc.cancelled.is_set()
```

## 容易走错的地方

### 失败模式 1：不测 wave 分区

`tool_concurrency_overrides` 配置错误时，SideEffect 工具可能被错误分到并行 wave。

### 失败模式 2：子代理测试只覆盖 happy path

不测超时和取消，生产中出现僵死子代理。

### 失败模式 3：依赖审计只做一次

新依赖可能带来安全公告或许可证冲突，CI 持续检查才能及时发现。

### 失败模式 4：忽略 ChatTurnQueue 竞态窗口

`try_start_or_enqueue` 的原子性是核心。只测分开的接口会遗漏竞态 bug。

## 证据来源（v0.1.38）

- 源码：`src/run_control.rs`、`src/chat_turn_queue.rs`、`src/tool_executor.rs`
- 测试：`tests/config_validation.rs`、`tests/db_integration.rs`、`tests/tool_permissions.rs`
- CI/CD：`.github/workflows/`、`deny.toml`

## 图表清单

### 图 15-1：MicroClaw 的测试金字塔

![图 15-1：MicroClaw 的测试金字塔](../assets/figures/fig-15-test-pyramid.svg)
